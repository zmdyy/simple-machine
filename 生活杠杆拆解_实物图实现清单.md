# 生活杠杆拆解：实物图实现清单

更新时间：2026-09-25

## 统一实现原则

本轮沿用羊角锤、鱼竿已经确定的思路：

- 真实实物图作为主视觉，不用抽象杆图替代。
- 物理标注（O、F₁、F₂、l₁、l₂）单独用 SVG 图层叠加，不把答案画死在图片里。
- 实物图与力学几何共用同一套坐标，避免“图片位置”和“计算位置”错位。
- 教师拆解按步骤揭示：先看实物，再找支点，再揭示动力/阻力，最后画作用线和力臂。
- 参数探究仍保留，但关键物理点的含义固定。
- 不新增外部图片，直接使用仓库已有实物素材。

## 1. 扫把

素材：`assets/life/broom.png`

固定物理含义：
- 支点 O：上手稳定扫把柄的位置。
- 动力作用点：下手推/拉扫把柄的位置。
- 阻力作用点：扫把头与地面作用的位置。
- 动力方向：沿扫地方向近似水平。
- 阻力方向：与扫地方向相反。

分步显示：
1. 只显示扫把实物图。
2. 标出“上手 · 支点 O”。
3. 标出“下手 · 动力点”和“扫把头 · 阻力点”，同时显示 F₁、F₂。
4. 后续显示杆轴、作用线和力臂。
8. 可改变下手位置，比较动力臂变化。
9. 进入“你来画”。

已实现：
- `broom.png` 作为主图。
- 三个关键点与实物图坐标绑定。
- 扫把保持“课眼”案例。

## 2. 开瓶器

素材：`assets/life/opener-lift.png`

固定物理含义：
- 支点 O：开瓶器鼻端压住瓶盖的位置。
- 阻力作用点：开瓶器下唇钩住瓶盖边缘的位置。
- 动力作用点：手柄握持/抬起的位置。
- 动力方向：手柄向上抬。
- 阻力方向：瓶盖对开瓶器的作用方向与抬盖方向相反。

分步显示：
1. 只显示真实开瓶器与瓶盖。
2. 标出支点 O。
3. 标出“盖沿 · 阻力点”和“手柄 · 动力点”，再叠加 F₁、F₂。
4. 后续显示结构轴线。
5. 显示 l₁、l₂。
8. 可改变手在柄端的施力位置。
9. 进入“你来画”。

已实现：
- 将开瓶器提升为“课眼”案例。
- 修正备用几何中的动力/阻力方向，使其与实物图逻辑一致。
- 强调“支点不是瓶子中心，而是鼻端与瓶盖的接触点”。

## 3. 小推车（独轮车）

素材：`assets/life/wheel.png`

固定物理含义：
- 支点 O：轮轴。
- 动力作用点：把手处。
- 阻力作用点：车斗中货物的重心位置。
- 动力方向：把手向上抬。
- 阻力方向：货物重力竖直向下。

分步显示：
1. 只显示小推车实物图。
2. 标出“轮轴 · 支点 O”。
3. 标出“把手 · 动力点”和“货物重心 · 阻力点”，并显示 F₁、F₂。
4. 后续显示杠杆结构轴线。
5. 显示 l₁、l₂。
8. 移动货物前后位置，比较阻力臂变化。
9. 进入“你来画”。

已实现：
- 案例名称改为“小推车（独轮车）”。
- 提升为“课眼”案例。
- 货物位置参数继续保留，用于控制变量探究。

## 本轮额外修复

- 参数变化时的“旧力臂”现在也使用 `LifeScenes.layout()` 的实物图坐标，不再使用另一套备用几何，避免对比力臂与图片错位。
- 切换到扫把、开瓶器、小推车时，右侧会先给出针对该实物的观察提示。
- `life-scenes.js` 与 `life-lab.js` 已通过 JavaScript 语法检查。
- `index.html` 已更新脚本版本号，减少浏览器继续使用旧缓存的情况。

## 下一轮建议审核点

1. 实际页面中三个关键点是否准确压在实物图对应部位。
2. 开瓶器支点是否需要再做 5–15 px 的微调。
3. 扫把下手位置变化时，点是否始终落在真实扫把柄上。
4. 小推车货物重心滑动范围是否始终处于车斗内部。
5. 投影到教室大屏后，支点、动力点、阻力点文字是否足够清晰。
